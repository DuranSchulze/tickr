## Rewrite `MemberRateDialog` — editable default rate + inline per-client rate list

**No backend/schema changes needed.** All server functions already exist:

- `updateMemberBillableRateFn({ memberId, billableRate: number | null })` — null falls back to workspace default
- `setMemberClientBillableRateFn` / `unsetMemberClientBillableRateFn` — per-client overrides (already used by the current dialog)
- Fallback chain already implemented in `computeBillableRate()` (`src/lib/time-tracker/billing.ts:26`): member-client rate → client default → member rate → workspace default

### 1. Rewrite `MemberRateDialog` in `src/components/time-tracker/MemberRow.tsx:67-343`

Replace the current two-pane layout (saved-rates list + combobox form) with a single-column dialog (`sm:max-w-lg`, scrollable body):

**Default rate section (top):**

- "Default hourly rate" input (`type="number" min="0" step="0.01"`, string state seeded from `member.billableRate` on open, `''` = null)
- Helper text: "Applies to clients without an override. Empty falls back to the workspace default (₱X/hr)" using `formatCurrency`
- Live "Effective default" line that updates as the user types

**Client rates section (below):**

- One row per client — all ACTIVE clients plus any non-ACTIVE clients that have a saved override (marked with a status badge), sorted by name
- Each row: client name + status badge on the left, secondary line showing the effective rate and its source (`₱300/hr · override` / `· client default` / `· member default` / `· workspace default`), and a right-aligned rate input (`w-28`, placeholder "Default") on the right
- Empty input = uses the default; rows with an active override get a subtle primary highlight (matches the existing Rate-cell badge styling)
- Empty state text when the workspace has no clients

**Save behavior (single "Save changes" button in the footer):**

- Dirty check compares _parsed_ values (so `"300"` vs `"300.00"` isn't dirty): default draft vs `member.billableRate`, each client draft vs its saved override
- Save button disabled while `pending`, not dirty, or any input invalid (`aria-invalid` on offending inputs)
- On save, sequentially `await`: `updateMemberBillableRateFn` if default changed, then set/unset per changed client with `effectiveFrom: todayKey(timezone)` (server rejects past dates) — sequential to avoid races on the overlapping date-effective rows
- Then update local state, reseed drafts, `void router.invalidate()`, success/error toast via `gooeyToast` (existing patterns)

**Cleanup:** drop now-unused imports (`Combobox`, `SuspendedClientWarning` if unused elsewhere in the file), add `updateMemberBillableRateFn` import.

### 2. Leave the rest alone

- The members-table "Rate" cell (already shows effective rate + "N client rates" label, refreshes via loader invalidate) — unchanged
- Pre-existing local modifications in `WorkspaceBillingPanel.tsx`, `EntryRow.tsx`, `useTimerKeyboard.ts` and the untracked test files — untouched

### 3. Verification

- Run the repo's lint/typecheck scripts to confirm no regressions
- Boot the dev server and visually verify the dialog on `/app/workspace/members` (open a member's Rate cell) if auth permits; otherwise report what was checked
