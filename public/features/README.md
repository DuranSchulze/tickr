# Feature Screenshots

Place feature screenshots here (PNG, WebP, or JPG) referenced by
`src/features/announcements/manifest.json`.

Recommended size: 600×400 px (3:2 aspect ratio).
Name them descriptively, e.g. `bulk-actions.png`, `performance.png`.
