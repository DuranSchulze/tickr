# ── pnpm configuration ───────────────────────────────────────────────────────
# Lockfile only — no frozen install on dev
# strict-peer-dependencies=false allows peer dep mismatches without failing

auto-install-peers=false
strict-peer-dependencies=false
